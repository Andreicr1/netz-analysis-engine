# Prompt G — Sprint Final de Coverage (fusão 2026-04-01)

## Contexto

Estado atual: 89.8% cobertura (158/176). Objetivo: 100% dos endpoints com valor real.

O sprint tem quatro partes:
- **Parte 1 — Wiring:** 6 endpoints desconectados com valor real
- **Parte 2 — Cleanup:** 9 rotas deprecated/duplicatas deletadas do backend
- **Parte 3 — Defer:** 6 endpoints mantidos no backend sem wiring (documentar)
- **Parte 4 — Correção de audit:** 1 falso negativo + recálculo final

**Não começar nenhuma implementação sem ler os arquivos indicados em cada fase.**

---

## PARTE 1 — Wiring Frontend (6 endpoints)

---

### G1 — Rebalancing Apply

**Endpoint:** `POST /rebalancing/proposals/{proposal_id}/apply`
**Retorno:** `PortfolioSnapshotRead`

**Contexto:**
O `RebalancingTab.svelte` já tem o fluxo propose/approve/execute via
`/portfolios/{profile}/rebalance/*`. O endpoint `/rebalancing/proposals/{id}/apply`
é uma rota separada em `rebalancing.py` que aplica diretamente os pesos ao
`ModelPortfolio.fund_selection_schema` e cria um `PortfolioSnapshot`.

**IMPORTANTE:** O backend verifica `if proposal.status != "pending": raise 409`.
O botão deve aparecer para eventos com `status === "pending"` — não "approved".
É o "fast path" que bypassa o governance flow de approve/execute.

**Arquivos a ler:**
```
backend/app/domains/wealth/routes/rebalancing.py          — handler completo
frontends/wealth/src/lib/components/RebalancingTab.svelte — todo o arquivo
backend/app/domains/wealth/schemas/portfolio.py           — PortfolioSnapshotRead
```

**O que implementar em `RebalancingTab.svelte`:**

1. Estado: `let showApplyDialog = $state(false)`

2. Função `handleApply`:
```typescript
async function handleApply(payload: ConsequenceDialogPayload) {
  if (!targetEventId) return;
  actionError = null;
  try {
    const api = createClientApiClient(getToken);
    await api.post(`/rebalancing/proposals/${targetEventId}/apply`, {});
    await loadRebalanceEvents();
    await invalidateAll();
  } catch (e) {
    if (e instanceof Error && e.message.includes("409")) {
      actionError = "Proposal is no longer pending — it may have already been applied.";
    } else {
      actionError = e instanceof Error ? e.message : "Apply failed";
    }
  }
}
```

3. Botão "Apply to Portfolio" visível quando `pendingEvent?.status === "pending"`,
   usando `ConsequenceDialog` com:
   - title: "Apply Rebalance to Portfolio"
   - description: "This will update the model portfolio weights immediately,
     bypassing the standard approval workflow. This action cannot be undone."
   - action: "Apply" | requiresRationale: true (min 10 chars)

---

### G2 — Screener Catalog Detail

**Endpoint:** `GET /screener/catalog/{external_id}/detail`
**Retorno:** `FundDetailOut` (campos enriquecidos: expense_ratio, is_index, is_target_date, AUM completo, disclosure flags)

**Contexto:**
O catalog browse (`screener/+page.svelte`) mostra itens mas sem drill-down.
A página `screener/[cik]` cobre apenas fundos SEC com CIK.
O `CatalogDetailPanel.svelte` pode já existir — verificar antes de criar novo componente.
Para ETFs, BDCs, private funds e UCITS, o detalhe vem deste endpoint.

**Arquivos a ler:**
```
frontends/wealth/src/routes/(app)/screener/+page.svelte         — ver CatalogTable e navegação atual
frontends/wealth/src/lib/components/screener/                   — verificar se CatalogDetailPanel existe
frontends/wealth/src/routes/(app)/screener/[cik]/+page.server.ts — padrão de detalhe existente
backend/app/domains/wealth/routes/screener.py                   — linhas 1662-1756, FundDetailOut shape
frontends/wealth/src/lib/types/catalog.ts                       — tipos existentes
```

**O que implementar:**

Se `CatalogDetailPanel.svelte` **já existe**: conectar ao endpoint enriquecido
`GET /screener/catalog/{external_id}/detail` em vez dos dados parciais atuais.

Se **não existe**: criar rota `screener/catalog/[externalId]/+page.server.ts` +
`+page.svelte` com fetch de `GET /screener/catalog/{external_id}/detail`.

Em ambos os casos, redirecionar fundos com `universe === "registered_us"` para
`/screener/{cik}` (que já tem todos os tabs de N-PORT, holdings, prospectus, etc).

Conteúdo do detalhe para outros universos:
- Header: nome, ticker/ISIN, tipo, gestor, AUM formatado (nunca `.toFixed()`)
- Grid: expense_ratio_pct, avg_annual_return_1y/10y, inception_date,
  is_index, is_target_date, is_fund_of_funds
- Badge de `disclosure` (has_holdings, has_nav, has_prospectus, has_13f_overlay)
- Botão "Import to Universe" → `POST /screener/import/{external_id}` (já existe)

---

### G3 — Strategy Drift Scan

**Endpoint:** `POST /analytics/strategy-drift/scan`
**Retorno:** `StrategyDriftScanRead` — `{ scanned_count, alerts_found, alerts }`

**Contexto:**
A página `/analytics` já mostra drift alerts via `GET /analytics/strategy-drift/alerts`.
O endpoint de scan usa advisory lock — retorna 409 se scan já em progresso.

**Arquivos a ler:**
```
frontends/wealth/src/routes/(app)/analytics/+page.svelte  — seção Strategy Drift
backend/app/domains/wealth/routes/strategy_drift.py       — linhas 84-135
```

**O que implementar:**

Botão "Scan Now" (ícone `ArrowsClockwise` do phosphor-svelte, `weight="light"`)
na seção de Strategy Drift em `analytics/+page.svelte`.

```typescript
async function triggerDriftScan() {
  scanning = true;
  scanResult = null;
  try {
    const api = createClientApiClient(getToken);
    scanResult = await api.post<StrategyDriftScanResult>(
      '/analytics/strategy-drift/scan', {}
    );
    await invalidateAll();
  } catch (e) {
    if (e instanceof Error && e.message.includes("409")) {
      scanError = "Scan already in progress — try again in a moment.";
    } else {
      scanError = e instanceof Error ? e.message : "Scan failed";
    }
  } finally {
    scanning = false;
  }
}
```

Após scan: toast ou inline `"Scanned {scanned_count} instruments, found {alerts_found} alerts."`

Tipo a adicionar em `lib/types/analytics.ts`:
```typescript
export interface StrategyDriftScanResult {
  scanned_count: number;
  alerts_found: number;
  alerts: StrategyDriftAlert[];
}
```

---

### G4 — Private Funds Tab no Manager Panel

**Endpoint:** `GET /sec/managers/{crd}/private-funds`
**Retorno:** `PrivateFundListResponse` — `{ funds, total_count, total_aum }`

**Arquivos a ler:**
```
frontends/wealth/src/routes/(app)/screener/managers/      — estrutura atual e tabs
backend/app/domains/wealth/routes/sec_funds.py            — linhas 141-215
backend/app/domains/wealth/schemas/sec_funds.py           — PrivateFundSummary, PrivateFundListResponse
```

**O que implementar:**

Tab "Private Funds" no manager detail panel. Lazy-load ao ativar (padrão das outras tabs):

```typescript
async function loadPrivateFunds() {
  if (privateFunds) return;
  loadingPrivate = true;
  try {
    const api = createClientApiClient(getToken);
    privateFunds = await api.get<PrivateFundListResponse>(
      `/sec/managers/${crd}/private-funds`
    );
  } catch { privateFunds = null; }
  finally { loadingPrivate = false; }
}
```

Tabela: `Fund Name | Type | AUM | Investors | Fund of Funds`.
AUM com formatador compacto do `@investintell/ui`.
Empty state: "No Schedule D private fund data available."

Tipos a adicionar em `lib/types/manager-screener.ts`:
```typescript
export interface PrivateFundSummary {
  fund_name: string;
  fund_type: string | null;
  gross_asset_value: number | null;
  investor_count: number | null;
  is_fund_of_funds: boolean | null;
}
export interface PrivateFundListResponse {
  funds: PrivateFundSummary[];
  total_count: number;
  total_aum: number | null;
}
```

---

### G5 — Style History Tab na Página [cik]

**Endpoint:** `GET /sec/funds/{cik}/style-history`
**Retorno:** `StyleHistoryResponse` — `{ snapshots, drift_detected, quarters_analyzed }`

**Arquivos a ler:**
```
frontends/wealth/src/routes/(app)/screener/[cik]/+page.server.ts — loader atual (6 fetches paralelos)
frontends/wealth/src/routes/(app)/screener/[cik]/+page.svelte    — tabs existentes
backend/app/domains/wealth/routes/sec_funds.py                   — linhas 441-497
```

**O que implementar:**

1. Em `+page.server.ts`, adicionar ao `Promise.allSettled`:
```typescript
api.get<StyleHistoryResponse>(`/sec/funds/${cik}/style-history`, { limit: '8' })
```
Retornar `styleHistory` no objeto de return.

2. Tab "Style" no TabBar existente. Conteúdo:
   - Badge de alerta se `drift_detected === true`
   - Timeline de snapshots (cronológica reversa): badge do `style_label`, barra
     horizontal equity/fixed_income/cash_pct, data do relatório
   - Footer: `{quarters_analyzed} quarters analyzed`
   - Empty state: "No style classification data available for this fund."

Tipos a adicionar em `lib/types/catalog.ts`:
```typescript
export interface StyleSnapshotItem {
  report_date: string;
  style_label: string | null;
  growth_tilt: number | null;
  sector_weights: Record<string, number>;
  equity_pct: number | null;
  fixed_income_pct: number | null;
  cash_pct: number | null;
  confidence: number | null;
}
export interface StyleHistoryResponse {
  snapshots: StyleSnapshotItem[];
  drift_detected: boolean;
  quarters_analyzed: number;
}
```

---

### G6 — Worker Triggers na Settings Page

**Endpoints (5):**
```
POST /workers/run-sec-13f-ingestion
POST /workers/run-sec-adv-ingestion
POST /workers/run-nport-ingestion
POST /workers/run-esma-ingestion
POST /workers/run-regime-fit
```

**Contexto:**
A página `settings/system/+page.svelte` já tem um array `WORKER_TRIGGERS` com
9 workers wired. A UI de trigger (botão + toast) já existe — só adicionar entradas.

**Arquivos a ler:**
```
frontends/wealth/src/routes/(app)/settings/system/+page.svelte  — localizar WORKER_TRIGGERS (~linha 89)
```

**O que implementar:**

Adicionar ao array `WORKER_TRIGGERS`:
```typescript
{ label: 'SEC 13F Holdings',   endpoint: '/workers/run-sec-13f-ingestion', scope: 'global' },
{ label: 'SEC ADV Managers',   endpoint: '/workers/run-sec-adv-ingestion', scope: 'global' },
{ label: 'N-PORT Holdings',    endpoint: '/workers/run-nport-ingestion',   scope: 'global' },
{ label: 'ESMA Fund Register', endpoint: '/workers/run-esma-ingestion',    scope: 'global' },
{ label: 'Regime Fit (HMM)',   endpoint: '/workers/run-regime-fit',        scope: 'global' },
```

Sem outras alterações. O componente existente renderiza automaticamente.

---

## PARTE 2 — Backend Cleanup (9 deleções)

Deletar apenas os handlers listados — não arquivos inteiros.
Antes de cada deleção, verificar se há testes referenciando o endpoint.
`make check` após cada arquivo modificado.

### `sec_analysis.py` — deletar 4 handlers

| Handler | Motivo |
|---------|--------|
| `GET /managers/search` | Duplicado por `GET /manager-screener/` (conectado) |
| `GET /managers/{cik}/holdings` | Duplicado por `GET /manager-screener/managers/{crd}/holdings` |
| `GET /managers/{cik}/style-drift` | Duplicado por `GET /manager-screener/managers/{crd}/drift` |
| `GET /managers/sic-codes` | Referência estática sem consumidor |

### `sec_funds.py` — deletar 1 handler

| Handler | Motivo |
|---------|--------|
| `GET /managers/{crd}/registered-funds` | Duplicado por `/manager-screener/managers/{crd}/registered-funds` |

### `documents.py` — deletar 1 handler

| Handler | Motivo |
|---------|--------|
| `POST /upload` (direct upload sem path param) | Legacy — frontend usa pre-signed URL flow |

### `funds.py` — deletar 2 handlers

| Handler | Motivo |
|---------|--------|
| `GET /scoring` | Deprecated — substituído por `/instruments` + `fund_risk_metrics` |
| `GET /{fund_id}/nav` | Deprecated — NAV via entity analytics |

Os 3 endpoints restantes de `funds.py` (`GET /funds`, `GET /funds/{fund_id}`,
`GET /funds/{fund_id}/risk`) continuam ativos até migração completa para instruments.

### `instruments.py` — deletar 1 handler

| Handler | Motivo |
|---------|--------|
| `POST /import/csv` | Frontend usa Yahoo import ou screener import |

---

## PARTE 3 — Defer (manter no backend, não conectar)

Endpoints com valor potencial mas cobertos por alternativas no frontend.
Manter no backend para consumo via API direta ou evolução futura.
**Nenhuma ação de código necessária** — apenas documentar no audit.

| Endpoint | Arquivo | Motivo para defer |
|----------|---------|-------------------|
| `GET /screener/securities` | screener.py | Vista pós-screening — valor quando fluxo de aprovação evoluir |
| `GET /screener/securities/facets` | screener.py | Acompanha securities acima |
| `POST /screener/import-esma/{isin}` | screener.py | Generic import cobre; ESMA enrichment é incremental |
| `GET /analytics/backtest/{run_id}` | analytics.py | Útil se backtest virar async com histórico de runs |
| `POST /analytics/optimize` | analytics.py | Single-point para API consumers (frontend usa Pareto) |
| `GET /analytics/strategy-drift/{instrument_id}` | strategy_drift.py | Frontend usa /history; pode servir webhook futuro |

---

## PARTE 4 — Correção de Audit

**`GET /risk/stream` é um falso negativo.**

O `risk-store.svelte.ts` conecta via:
```typescript
const sseUrl = config.sseEndpoint ?? `${apiBase}/risk/stream`;
sseConnection = createSSEStream({ url: sseUrl, getToken, ... });
```

O scanner de audit procura `api.(get|post|...)` e não detecta `createSSEStream(url)`.
O endpoint está conectado e funcionando. **Nenhuma alteração de código necessária.**

**Atualizações em `docs/audit/endpoint-coverage-audit-2026-04-01.md`:**

1. Mover `GET /risk/stream` de DISCONNECTED → CONNECTED
   (Backend: `risk.py` | Frontend: `risk-store.svelte.ts` via `createSSEStream`)

2. Após Parte 2 (9 deleções do backend), o total muda:
   ```
   Total endpoints backend wealth (excl. infra/workers): 167  (176 - 9 deletados)
   Conectados: 164  (158 + 1 risk/stream + 5 worker triggers + 5 wiring novos - 5 deletados connected)
   Desconectados (deferred): 6
   Infra/workers (excluídos): 28
   Phantom calls: 0
   Coverage: ~96.4% (excluindo deferred como "intended gap")
   ```

3. Atualizar tabela "Coverage by Route File":
   - `risk.py`: 6/7 → 7/7 (100%)
   - `sec_analysis.py`: 5/9 → 5/5 (100% dos remanescentes)
   - `sec_funds.py`: 6/9 → 6/8 (75%, private-funds + style-history agora connected)
   - `instruments.py`: 7/7 → 6/6 (csv deletado)
   - `documents.py`: 6/7 → 6/6 (direct upload deletado)
   - `funds.py`: 3/5 → 3/3 (scoring + nav deletados)

4. Adicionar seção **DEFERRED (6 endpoints)** no audit entre DISCONNECTED e INFRA.

5. Adicionar nota: "`GET /risk/stream` conectado via `createSSEStream` no
   `risk-store.svelte.ts` — padrão não detectado pelo scanner `api.(get|post|...)`.
   Workers wired na settings page: 14/28."

---

## Regras críticas

- **Ler antes de implementar:** Cada G# lista arquivos obrigatórios
- **Gate:** `pnpm run check` (wealth frontend) após G1-G6. `make check` após Parte 2
- **Sem novos endpoints:** Apenas wiring e cleanup
- **Deleções precisas:** Só os handlers listados, nunca arquivos inteiros
- **SSE:** sempre `fetch()` + `ReadableStream`, nunca `EventSource`
- **Formatação:** sempre `@investintell/ui` formatters, nunca `.toFixed()`

## Definition of Done

- [ ] G1: Botão "Apply to Portfolio" em RebalancingTab (status === "pending"), ConsequenceDialog
- [ ] G2: Catalog detail conectado ao endpoint enriquecido (ou nova rota), redirect para [cik] quando registered_us
- [ ] G3: Botão "Scan Now" em analytics/+page.svelte com inline result
- [ ] G4: Tab "Private Funds" em manager detail (lazy load)
- [ ] G5: Tab "Style" em screener/[cik] com timeline trimestral
- [ ] G6: 5 worker triggers adicionados ao array em settings/system
- [ ] Parte 2: 9 handlers deletados do backend, `make check` verde
- [ ] Parte 4: Audit atualizado — risk/stream em CONNECTED, seção DEFERRED criada, números recalculados
- [ ] `pnpm run check` 0 erros no wealth frontend
- [ ] `make check` verde no backend
