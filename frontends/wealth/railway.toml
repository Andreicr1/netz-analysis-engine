[build]
builder = "railpack"
buildCommand = "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @netz/ui build && pnpm --filter netz-wealth-os build"

[deploy]
startCommand = "node frontends/wealth/build/index.js"
