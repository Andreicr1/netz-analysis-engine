[build]
builder = "railpack"
buildCommand = "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @netz/ui build && pnpm --filter netz-wealth-os build"

[deploy]
startCommand = "node build/index.js"
