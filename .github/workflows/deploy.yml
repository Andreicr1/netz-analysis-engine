name: CI on push

on:
  push:
    branches: [main]

jobs:
  # Backend CI (Railway auto-deploys on push — this just runs tests)
  ci:
    uses: ./.github/workflows/ci.yml

  # Frontends: Railway auto-deploys on push via railway.toml per service
  # No GitHub Actions deploy needed — Railway watches the repo directly
