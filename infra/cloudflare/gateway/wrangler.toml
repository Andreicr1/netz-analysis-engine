name = "netz-gateway"
main = "src/index.ts"
compatibility_date = "2025-12-01"

[vars]
BACKEND_ORIGIN = "https://netz-backend.andrei-rachadel.workers.dev"

# Service binding to backend container (same Cloudflare account)
# [[services]]
# binding = "BACKEND"
# service = "netz-backend"
